# check-paths.sh 豁免清单 —— 一行一条路径前缀。
# 规矩：每条都要写清**为什么豁免、什么时候去掉**。挂着任务号的临时豁免才算数，无限期的不算。

# ── 通常该永久豁免的 ──
src/                       # 源码里的示例 IP / 网段是内容，不是机器配置
archive/                   # 冻结的历史，一个字都不该改
claude-outputs/            # 草稿区：截图说明、报告、记录——当时的事实
product/                   # 需求原话逐字保留

# ── 临时豁免写在这里，**必须挂任务号** ──
# <路径>/                  # <为什么> → T-<号>。做完删掉这行
