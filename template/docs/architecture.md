# 架构设计说明

> **新项目必填。** 新会话靠这一份知道「这个系统由哪几块组成、它们怎么连起来」。

## 一、分层 / 组成

`<每一块是什么、用什么技术、为什么选它>`

## 二、进程与边界

`<哪些是常驻进程、谁起谁、崩了会怎样>`

## 三、对外接口

`<入口在哪、鉴权边界在哪、端口规划>`

## 四、关键机制的实现逻辑

`<按功能分小节。每一节末尾指向 ai/specs/ 里对应的方案>`
