# The link-integrity allowlist: one full path per line, matched exactly; a line ending in `/` ignores that whole prefix.
# Only put in cases where the path really should not exist but the reference is correct (a historical note mentioning
# a file that has since been deleted, for instance).
# Before adding a line, ask once: should that reference be fixed instead? Usually it should.
#
# 🔴 **This file is project content (class seed, never overwritten by a sync)**: what it lists are **your project's own**
# historical paths, none of which hold on another project -- syncing it as part of FRAME would push one project's
# history onto every project.
#
# The three common kinds (add your own as needed):
#  . a path relative to a language package (`ai/xxx.go` and the like, really living in some package under `src/`)
#  . paths that only exist on a real machine or runtime (`tmp/...`, `ops/....py`)
#  . archived old documents (historical prose is kept as written, so the reference is not changed)

# -- "paths inside the template" mentioned in the Maintainer handbook (only a repo owning the templates has these) --
# These two exist only inside ai/template/ and ai/template-en/ (they travel with the template to a new project),
# so not resolving from the repo root is normal.
ai/glossary.md
ai/frame-manifest.txt

# -- ignore a whole prefix (ending in /) --
# Listed one by one, every new `ai/template/...` reference in the Maintainer handbook slips through, and the symptom
# only appears in a consuming project (which has no template directory) -- measured as 3 broken links exactly this way.
ai/template/
ai/template-en/
