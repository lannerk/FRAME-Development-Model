# Project memory - volume: traps in writing criteria / scripts (type 4)

> When `ai/memory.md` hits its cap it is **split into volumes** here — no rows deleted, no cap raised
> (the criterion is in `ai/memory.md`, "what to do when it is full").
> **When to read it**: before touching a guard or a criterion, before writing a regex / `tr` / `sed` / `awk`, before changing template sync.
> **Rules**: append only, never delete a row (the guard `check-ledger.sh` checks "entries may only grow"); every row carries the date it was verified; **numbers carried over are not renumbered**.
> **The filename may be localized** to the project's working language; the template keeps the language-neutral `type4.md`
> because the two templates' **paths must match character for character** (`check-template-sync.sh` checks it).


| # | What it records | Source · **verified on** |
|---|---|---|
| | | |
