# Architecture notes

> **Required for a new project.** A new session relies on this one file to know "which pieces this system is made of and how they connect".

## 1. Layers / components

`<what each piece is, what technology it uses, why that one was picked>`

## 2. Processes and boundaries

`<which ones are long-running processes, who starts whom, what happens when one crashes>`

## 3. External interfaces

`<where the entry points are, where the auth boundary is, the port plan>`

## 4. How the key mechanisms are implemented

`<one subsection per feature. Each one ends by pointing at the matching spec in ai/specs/>`
