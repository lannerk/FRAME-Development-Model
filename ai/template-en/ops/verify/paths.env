# ops/verify/paths.env —— **本项目专属的路径与前缀**，给可选的前端类守门用（**初始化时按需填**）。
# 🔴 为什么要这份：FRAME 本体与具体项目无关，所以守门里**不许写死**某个项目的目录名；
#    值放在这一份里，它是**项目内容（seed）**，同步永不覆盖它。
# 一行一个 KEY=值，被 `.` 进脚本。**没配的守门会自己跳过**（SKIP 不是红）。
# 没有前端、没有原型镜像的项目：整份留着注释就行。
#
# check-mirror：前端源 ↔ 原型镜像（两份都要配，缺一个就跳过）
# MIRROR_SRC=src/<项目>/web
# MIRROR_DST=product/design/prototype/<项目>/<页面集>
#
# check-cachebust：挂脚本的那一页 ＋ 这一族脚本的名字前缀
# CACHEBUST_WEB=src/<项目>/web
# CACHEBUST_PREFIX=<前缀>-
# CACHEBUST_PAGE=src/<项目>/web/index.html
#
# check-css-namespace：前端目录（不写就用 CACHEBUST_WEB）
# CSSNS_WEB=src/<项目>/web
# CSSNS_PAGE=index.html
#
# ai/check-links.sh：语言包所在的源码前缀（判引用时多试一层）
# LINKCHECK_SRC=src/<项目>
