# R&D 记忆 —— 做研究时才用得上、但每次都用得上的事

> **只有研究席写，上限 80 行。** 格式与 `ai/memory.md` **一字不差**——七类 · 每条带「哪天验的」·
> **只追加不删行** · 🔴 **凭据本身不许落盘**（只记「哪个 token · 在哪找 · 粘到哪个框」）。
> 它是**线级的、跨话题的**：换个话题还带得走。满了把成段的挪进对应话题的 `notes/`，正文只留一行指路。

🔴 **跨话题且属于全项目的**（需求方的偏好红线、环境怪癖）**不记在这里**——
`ai/memory.md` 研究席**只读不可写**，发信给监督席转记，这里只留一行指路。

## 只记这七样

| # | 记什么 |
|---|---|
| **1** | 经常要用的必要信息（数据源在哪 · 用哪个账号 · 去哪找） |
| **2** | 特定情况按特定方式处理（「遇到 X → 按 Y 办」＋一句为什么） |
| **3** | 自己摸索出来的解法（下次同样的事照它办） |
| **4** | 踩过的错法（查证过的：「这样是错的：…… 正确是：……」） |
| **5** | 需求方在 R&D 上的偏好与红线（要什么格式 · 哪些事必须他本人做） |
| **6** | 环境与平台的怪癖（非文档化的事实，不是错法） |
| **7** | 术语对照（他口里的词 ↔ 实际的东西） |

## 条目

| # | 事 | 哪天验的 |
|---|---|---|
| | `<一条一行，只追加>` | `<YYYY-MM-DD>` |
