# 项目记忆 · 卷宗：写判据／写脚本的坑（类型 4）

> `ai/memory.md` 顶满时**分卷**到这里，**不删行、不放宽上限**（判据见 `ai/memory.md`「满了怎么办」）。
> **什么时候读**：动守门、动判据、写正则／`tr`／`sed`／`awk`、改模板同步之前。
> **规矩**：只追加不删行（守门 `check-ledger.sh` 查「行数只许增」）；每条带「哪天验的」；**搬过来的编号不重排**。
> **文件名可以按沟通语言改**（本项目例：`type4-写判据的坑.md`）；模板里用语言中性的 `type4.md`，
> 因为两份模板的**路径必须一字不差**（`check-template-sync.sh` 在查）。


| # | 记什么 | 来源 · **哪天验的** |
|---|---|---|
| | | |
