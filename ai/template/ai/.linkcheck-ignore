# 引用完整性检查白名单：一行一条完整路径，精确匹配；以 `/` 结尾的是**整段前缀忽略**。
# 只放「确实不该存在、但那句引用是对的」的情况（例如记录历史事件时提到一个已删的文件）。
# 放进来之前先问一句：是不是应该改掉那条引用？多数情况是。
#
# 🔴 **这一份是项目内容（seed，同步永不覆盖）**：里面登记的是**你这个项目自己的**历史路径，
# 换个项目一条都不成立——把它当 FRAME 本体同步，等于把一个项目的历史推给所有项目。
#
# 常见的三类（按需自己加）：
#  · 语言包内的相对路径（`ai/xxx.go` 这种，真实位置在 `src/` 下的某个包里）
#  · 只存在于真机 / 运行环境里的路径（`tmp/…`、`ops/….py`）
#  · 已归档的旧文档（历史正文照原样保留，不改引用）

# ── 维护席手册里提到的「模板内部路径」（拥有模板的仓库才有这两条）──
# 这两份只存在于 ai/template/ 与 ai/template-en/ 里（它们跟着模板去新项目），
# 从仓库根解析不到是正常的。
ai/glossary.md
ai/frame-manifest.txt

# ── 整段前缀忽略（以 / 结尾）──
# 逐条列的话，维护席手册每加一条 `ai/template/...` 引用就漏一条，而症状只在消费项目里出现
# （那边没有模板目录），拥有模板的仓库永远是绿的——实测过 3 条断链就是这么漏的。
ai/template/
ai/template-en/
