# 目录结构说明 + 改一处要同步哪里

> **新项目必填，而且要跟着目录变动一起改**——一份过时的目录地图比没有更糟。

## 目录

`<逐个目录一句话。顶层那张表 CLAUDE.md 里有，这里写细的>`

## 改一处要同步哪里

> 这一节是本文件真正的价值：**哪些东西天生成对**，改一个必须改另一个。

| 改了这个 | 必须同步 | 不同步会怎样 |
|---|---|---|
| `<例：前端文件>` | `<例：原型镜像 + 版本号>` | `<例：浏览器吃缓存，表现为「改了没效果」>` |
| | | |
