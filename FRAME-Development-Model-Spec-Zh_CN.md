# AI 四权分立开发模式

### FRAME Development Model

**F**our-**R**ole · **A**udited · **M**aintained · **E**vidence-based

> **一句话**：把 AI 协作拆成**四个互相独立、互相制衡的席位**——执行、审查、监督、维护，
> 再把协作状态**全部外化到文件**。于是会话随时可以换，工作不断线，而且没有人能既当运动员又当裁判。

**为什么叫 FRAME**：框架要**四条边**才成形，少一条就散架——
而第四条边（维护席）正是让另外三条**保持方正**的那条：规矩会过时、会被绕过、会和实际做法两张皮，
没人专职维护它，前三席迟早各做各的。
四个词各对应一条承重原则：**Four-Role** 四权分立 · **Audited** 审查独立 ·
**Maintained** 规矩有人养 · **Evidence-based** 一切结论带依据。

---

## 一、四权分别是什么

| 席位 | 权 | 干什么 | 制衡它的是 |
|---|---|---|---|
| **开发席** | **执行权** | 把功能可靠地实现出来 | 审查席可以打回；但它能带依据反驳 |
| **审查席** | **审查权** | 定标准、判过不过、排活、对需求方收需求；**同时是专业测试席** | 需求方可以否决；开发席可以带依据推翻它 |
| **监督席** | **监督权** | 独立盘查全项目，直接对需求方负责 | 意见须经审查席核实才能进队列 |
| **维护席** | **规制权** | 维护这套模式本身：角色手册、规则、模板、守门 | 需求方确认才动；**不参与开发，不碰需求** |
| 需求方 | **拍板权** | 定方向、拍取舍 | —— 它是权力来源 |

**效力顺序**：

```
需求方拍板  >  审查席验证过的事实  >  监督席的意见  >  任何人的「我觉得」
规矩本身怎么定   →  维护席（需求方确认后）
```

**分权不是分工。** 分工只是把活切开，分权是**让判断的人和被判断的人不是同一个**。
这套方法真正的价值在后半句。

**第四席解决的是另一个问题**：前三席都在**用**规矩，没有人在**养**规矩。
规矩一旦没人养，就会出现「文档这么写、实际那么做」——而这种偏离**没有任何症状**，
直到某天有人照着文档做了一遍才发现文档早就不对了。

---

## 二、它想解决的那个问题

你和一个 AI 会话聊着聊着，做出了不少东西。然后：

- 会话满了，新开一个，**它什么都不知道**。你把背景重述一遍，它漏掉了三件事里的一件。
- 你上周说过「这个不要做」，两周后它又做了一遍——**那句话只存在于一段已经滚走的对话里**。
- 它写完代码,自己说「测试通过，没问题」。**它既是写的人，也是判的人**。
- 你连发三条 bug，它回你一段总结，处理了两条。**第三条蒸发了，谁都不知道。**
- 你问「还剩什么没做」，它得把整个项目重新读一遍才能回答，而且答得不全。

这些都不是模型能力问题，是**协作结构**问题。
一个人（或一个会话）同时当作者、审稿人、记录员和规矩制定者，本来就不成立——**人类工程里早就不这么干了**。

四权分立做的事只有一件：**把「谁知道什么」从对话里搬到文件里。**

---

## 三、四个席位怎么协作

| 席位 | 一句话职责 | 它看什么 | 它的产出落哪 |
|---|---|---|---|
| **开发席** | 可靠地把功能实现出来（不是「实现出来」就行） | 自己那条任务 + 它的方案 | 源码 · 任务文件的「开发者回报」 |
| **审查席** | 资深技术专家**兼专业测试与设计**：审代码、审方案、**测功能与 UI**、**定架构/数据库/选型**、收需求、排活 | 待审的任务 + 现状 | 任务文件的「审查结论」「测试结论」· 决策台账 · 技术方案 · 设计与计划 |
| **监督席** | 需求方的全能技术顾问，单独沟通，不占用开发主线 | 你交给它的那个具体问题 | 独立产出目录（原样入库，任何人不改一个字） |
| **维护席** | 维护这套模式本身：规则、角色手册、模板、守门。**不参与开发，不碰需求** | 维护记录 + 四份规则 + 四份角色手册 | 规则与手册本身 · 维护记录 · 两份通用模板 |

### 五条让它真的能运转的规则

**1. 你只跟审查席说话。**
bug、要做的东西、问题、临时决定——全说给它。它负责翻译成任务、规格、决策。
你不需要对三个会话各说一遍，也不需要记得哪件事告诉过谁。

**2. 开发席和审查席写同一个文件。**
这是整套方法的承重墙。一条任务一个文件，审查席在上半部分写「要做什么 / 怎么做（建议，可反驳）/ 怎么算过」，
开发席在下半部分写「做了什么 / 依据 / 实测 / 自检输出 / 与建议不同之处」，审查席再写「审查结论」。

于是：**一件事的全部上下文在一个文件里**。中间插新任务不影响老的；
任何一方的会话没了，新会话打开这个文件就能接上——**它不需要知道之前聊过什么**。

**3. 维护席只管规矩，不碰活。**
它维护角色手册、规则、模板和守门脚本，**不参与开发、不讨论需求**。
你跟它说的话**不是需求**——是「这套模式该怎么改」。确认之后进它的**维护记录**（和收件簿是两条线）。
规矩改了，它负责把三席的手册同步改掉，并**开新会话验一遍他们是不是真按新规矩走**。

**改规范 = 改两处**：本项目改一条，通用模板（`ai/template/`）**当轮跟上**，守门脚本比 mtime，落后就报红。
进模板的判据只有一句：**换一个项目，这条还成立吗？**——成立的（做事的方法、分工、目录职责、通用的坑）进；
不成立的（产品是什么、机器与路径、编号、某次事故的细节）不进。同步时**留道理、换例子**。

**4. 审查席同时是测试席，而测试和审查是两个口令。**
「审查」＝核对他说的对不对（读码、对依据、跑守门）；「**审查并测试**」＝再把能测的都测一遍：
功能、UI 验收、接口、**端到端操作**、性能、用例覆盖、回归、必要时全量。
**要测什么是通用的，怎么测是每个项目自己的事**——有的在浏览器点，有的要连测试机远程操作，
有的要挂调试器进引擎；所以审查席开测前先制定本项目的测试手段，落成一份可维护的文档。
开发席那边也有一层：**做完先自测**（按验收逐条走、UI 对着定稿原型一比一），自测不过不许交审。
**两层的价值不同**——自测是「我做的我看过了」，审查席测是「**没写过这段代码的人来挑毛病**」。

**5. 监督席的意见不直接进开发队列。**
它的产出先给你，你转给审查席，审查席**逐条核实**（读源码、跑实测）之后才决定要不要变成任务。
三方有分歧时，**分歧只进审查席的核实记录，不进给开发的任务文件**——
开发席不需要知道谁对谁错，只需要知道最后定的是什么。

### 制衡怎么落地：依据 > 资历

**任何一席都可以反驳另一席，前提是带着源码行号、实测数字或官方文档来。**
开发席推翻审查席的建议是常事，也是被鼓励的——只要它在「与建议不同之处」里写清验证过程。
审查席被推翻了要在审查记录里认下来。

**没有「因为我是审查所以我说了算」这回事**——那样四权分立就变成了四层汇报。

---

## 四、六个核心机制

### 1. 任务状态机 —— 任何时候都知道「卡在哪」

```
待实施 ──认领──▶ 开发中 ──回报──▶ 待审 ──审查──▶ 已过 ──▶ 归档
   ▲                                  │
   └────────── 打回（写明原因）◀───────┘

挂起：阻塞于外部条件（必须写清等什么、等到了谁来解锁）
作废：需求变了或被别的任务取代（写明被谁取代）
```

状态只有六个，改状态要同时改两处（任务文件头 + 任务索引）。
**「还剩什么没做」从此是一个 40 行文件能回答的问题**，不是一次全项目扫描。

### 2. 收件簿 —— 你说过的每一句都有一行

你说的每件事，审查席**当轮**记进收件簿：一句一行、**逐字摘录不复述**、每行必须有处置
（转任务 / 转规格 / 当场答复 / 记决策 / 不做并写明原因）。**一轮结束前「待处理」必须清零。**

为什么逐字：复述会把**目的**丢掉，只剩表面动作——而目的才是判断做法对不对的依据。
为什么当轮：「等我整理好再记」的那一刻，就是丢失发生的时候。

**你提的每一件事都要听到回音，哪怕回音是「不做」。**

### 3. 一切结论带依据

写进任何文档的事实性结论，必须能指出出处：**源码行号 / 实测数字 / 官方文档**，三者取一。
两条推论：

- **引用代码行号之前先看文件 mtime**——读了旧版本下错结论，是最常见的翻车方式。
- **否定性结论要连「试的是哪种形态」一起写**——「不支持」和「我这样试不支持」是两回事。

### 4. 守门脚本，而且守门自己要被验

规矩靠人记，三个月后就没了。所以能机器查的规矩一律写成脚本，进交付前自检清单。
但更要紧的是那条推论：

> **新加的守门脚本或守门测试，必须当场故意造错跑一次，把「它确实红了」的输出贴出来。**

因为**守门脚本本身也是代码，也会错，而它错了没有任何症状**——自检永远绿，你以为被保护着，其实没有。
这不是形式主义，是这套方法里最容易被跳过、代价也最大的一步。

### 5. 每段收尾四件

1. 更新「现状」文件被这段改动的段落
2. 把这段动过的**每一条决策的实现列改掉**（不改的后果：没人能看出还剩什么）
3. 任务状态推到「待审」，回报五小节写完，自检原始输出存档
4. 写一行 commit message（这段动了什么 + 对应哪几条决策）

**四件缺一件，下一段接手的人就得自己翻代码重建现状——那比写这四件贵得多。**

🔴 **「一段」可以装好几单。** 把一个任务推到「待审」是**交出去**，不是这一段结束——
接着开下一单，**审查是异步的**。这四件是**一段只做一次**，在真的要停下来的时候做。

*为什么要专门写这句*：不写的话，「每段收尾……任务状态推到待审」会被读成「交一单＝一段结束」，
于是排着的后续任务一单不动，而这么读**完全合乎字面**（真发生过：推完一单就收尾，后面三单没动）。

### 6. 验收清单是唯一的考卷 —— 它的颗粒度决定缺陷的下限

清单**同时是规格和考卷**：建造的人照它做，审查的人照它验。
所以清单写漏的地方，**两边都发现不了**，一直到使用的人亲手点开才暴露。
加多少「要认真」都没用——**要改的是清单的写法**。

**① 一条一句。** 一行只许有一个要求；有两个就拆成带编号的子项。回报必须**逐子项对答**，缺一项就是打回。
*为什么*：一行里写「只列 A；并且按钮叫 B、就地展开、不跳走」，建造方做了 A、实测也只贴 A，
就能报「逐条实测过」——**而审查方照同一行验，验不出来**。

**② 图不是规格，清单才是。** 设计稿上画了而清单没写的，**等于没要求**。
所以设计定稿时要做一次「**图 ↔ 清单**」对齐：图上有的，要么写进清单、要么从图上删。
*为什么*：两份制品各说各话时，建造方照清单做、审查方照清单验，**图上那部分没有任何人负责**。

**③ 要求了过程，判据就要验过程。** 凡是规格里写了「异步 / 走任务 / 有进度 / 分步做」的，判据不许只验终态。
*为什么*：「装完之后 X 可用」这种判据，**一把梭同步做也能满足**。终态相同、过程缺失，是最容易验过去的一类。

**④ 搬运型任务要写明「搬完之后仍然不能用什么」。** 重构、搬家、换底座这类任务，
交付时一并说清哪些功能还在别的任务里没做。
*为什么*：使用方看到界面出现了就当成品试，**发现的「缺陷」其实是没排期**——浪费一轮来回，还消耗信任。

🔴 **推论（给审查席的）**：打回一条缺陷之后，除了修缺陷，要回头问一句——
**清单那一行为什么没拦住它**。**缺陷是症状，清单的颗粒度才是病。**

---

## 五、和「一个会话 vibe coding」比，好在哪

先说公道话：**做一个一次性脚本、试一个想法、改一个小 bug，单会话更快，别用这套。**
下面说的是「一个项目要做好几个月」这种场景。

| | 单会话 vibe coding | FRAME 四权分立 |
|---|---|---|
| **上下文在哪** | 在对话里。会话一换就没了 | **在文件里。会话是可替换的执行单元** |
| **开新会话的成本** | 重述背景，而且必然漏 | 说一个词（「你是开发者」），它自己读 |
| **谁来判对错** | 它自己。既写又判 | **审查席独立核**，读源码、跑实测、可打回 |
| **规矩会不会失效** | 没有规矩，或者规矩在对话里 | **维护席专职养**，改完开新会话验三席是不是真照做 |
| **你说的话会不会丢** | 会。你得自己记得追问 | 收件簿一句一行，一轮结束前清零 |
| **「还剩什么没做」** | 要它重新扫一遍，答不全 | 一个任务索引，40 行 |
| **结论可不可信** | 「我觉得应该没问题」 | 必须带源码行号 / 实测数字 / 官方文档 |
| **中断了怎么办** | 从头解释一遍 | 打开那个任务文件，接着写 |
| **token 成本** | 每次重述背景，越聊越贵 | 开场固定读几百行（`ops/verify/check-budget.sh` 实测），其余按需 |
| **三个月后回来** | 基本要考古 | 读「现状」+ 任务索引，十分钟进入状态 |

### 最关键的三点

**一、独立的审查权，不是「让 AI 再检查一遍」。**
同一个会话里说「你再检查一下」，它检查的是**自己刚才的思路**——它会倾向于确认自己。
审查席是另一个会话，**没有写代码时的那套假设**，它拿到的是结果和依据，只能靠核实来判断。
这一点上它和人类的代码评审是同一个道理：**评审的价值来自独立性，不来自认真程度。**

**二、规矩有人养，所以它不会慢慢失效。**
任何一套协作规矩，用着用着就会和实际做法分叉——文档写 A，大家做 B，而**没有任何报错**。
前三席都在用规矩，没有人在养规矩；第四席专门干这个：改规矩、同步模板、**开新会话验他们是不是真照做**。
这一席是这套方法能用一年而不是用三周的原因。

**三、文件即真相，所以会话是廉价的。**
一旦所有状态都在文件里，「会话满了」「模型换了」「昨天那个窗口关了」全都不再是事故。
你可以随时开三个新会话、关掉旧的、中途换人做另一件事再回来——**工作不断线**。
vibe coding 里最贵的东西是「上下文」，这套方法把它从会话里搬出来，于是它不再贵。

### 代价，说清楚

- **前期要填几个空**（项目介绍、现状、机器清单、自检清单），大概十分钟。
- **每段收尾有四件事要做**，不能省——省了就等于退回 vibe coding，还多背了一套目录。
- **小事上它是负担**。改个错别字不需要开任务文件。判据：**这件事会不会有人在两周后问起？**
  会 → 开任务；不会 → 直接做。

---

## 六、使用手册：你要做的只有四件事

### 1. 开会话，说一个词

开会话，各发一句 `你是开发者` / `你是审查者` / `你是监督者` / `你是维护者`，**不要附加任何背景**。
根目录的 `CLAUDE.md` 会自动加载，把它送到对应的角色手册，它自己读完现状和任务队列就开工。

**它如果回你「请问要我做什么」，说明哪里断了**——那是设计问题，不是它的问题，去查 `CLAUDE.md` 的身份表。

### 2. 有事只跟审查席说

bug、新需求、疑问、临时决定，一股脑说给审查席。它负责记进收件簿、翻译成任务、排优先级、回你去处。
**你不需要记得哪件事说过没有**——收件簿会告诉你。

它会拿选择题来找你拍板（两三个方案 + 各自代价 + 它的推荐与理由）。
**它不应该拿开放式问题来问你**「你觉得该怎么做」——那是它的活。

### 3. 让开发席干活

对开发席只需要说 `审查完毕请继续`。它自己去队列里拿优先级最高的那条。

### 4. 要不要测，你用口令决定

对审查席说 `审查`，它只审：读回报、核依据、读码、跑守门。
说 `审查并测试`，它还要**真跑真点**——功能、UI 验收、接口、端到端操作、性能、回归，发版前还有全量。
**测试比审查贵得多**，所以这是两个口令而不是一个：日常小改说「审查」，
要交付、要上真机、要给别人看的时候说「审查并测试」。

### 5. 需要你本人动手的，它会攒着一次问

插线、贴密钥、重启、验一个只能人在现场看的现象——这类事它会攒成一批，
每条写清**要你几分钟、能解锁什么**。
反过来，**它有权限自己做的事不该推给你**（「请你跑一下这条命令」不是解决方案）。

---

## 七、目录讲解

```
<项目名>/
├── CLAUDE.md              ← 自动加载的入口：身份表、口令、铁律、目录一眼
├── README.md              ← 给人看的项目简介
│
├── ai/                    ── 三席协作的一切
│   ├── index.md           全项目地图（开场必读，≤80 行）
│   ├── roles/             三份角色手册：开发者 / 审查者 / 监督者
│   ├── rules/             laws 铁律 · conventions 工程约定 · workflow 协作流程 · layout 目录规范
│   ├── tasks/             **一任务一文件** + 索引 + 归档 ← 承重墙在这里
│   ├── decisions/         决策台账（按编号段分文件，每条带主题标签）
│   ├── specs/             技术方案（定案后＝开工必读，随实现更新）
│   ├── state/             now.md 现状（≤200 行，每段收尾更新）
│   └── template/          开新项目用的通用副本（整个拷走即可）
│
├── product/               ── 产品定义
│   ├── vision.md          产品愿景（决策台账的上位依据）
│   ├── requirements/      **inbox.md 收件簿** · verbatim 原话 · specs 规格 · formal 正式需求
│   └── design/            prototype 原型 · ui 基线资产 · reference 参考
│
├── docs/                  ── 工程文档：架构 · 目录说明 · 功能清单 · 运维 · 真机链路
├── src/                   ── 源码（一个项目一个子目录）
├── ops/                   ── 部署运维 · **machines.json 机器清单** · **paths.ps1 路径定义** · verify 守门
├── dist/                  ── 构建产物（不入库）
├── claude-outputs/        ── AI 草稿区：截图 · 实测 · 报告 · 监督产出（**入库，是证据**）
├── archive/               ── 不再维护的历史（只进不出）
└── tmp/                   ── 一次性工作区（不入库，谁产生谁清）
```

### 三条贯穿整个目录的原则

**1. 按「有没有生命周期」分，不按「谁写的」分。**
有状态、会被反复引用的（任务/决策/方案/需求）→ 各自的固定目录；
一次性的、属于某个角色的（报告、截图、实测）→ 草稿区。

**2. 一件东西只有一个家。**
待办只有任务索引，交接只有现状文件，机器信息只有 `machines.json`，路径只有 `paths.ps1`。
**不许开第二本**——同一件事记在两处，状态永远对不上。

**3. 每个文件都有「读它的理由」和一个行数上限。**
索引 80 行、角色手册 150 行、任务文件 150 行、现状 200 行。
超了就分。**预算不是洁癖，是成本**——文档没有上限，一两年就会涨到几 MB，
那时任何会话读上两个就是几十万 token。

### 不入库的只有两个目录

`tmp/`（一次性）和 `dist/`（构建产物）。**其余一切都入库。**
要造一次性文件就造在 `tmp/` 里——**不要在别处造完再回来加一条忽略规则**，那是仓库长垃圾的标准路径。

**别把「一次性」和「随手产出」搞混**：方案、审查记录、截图、实测数据**看着像一次性产出，其实要留**，
是以后复核的依据；打包发布过程中生成的东西**看着很重要，其实用完就没用**，随时能重新构建。

---

## 八、怎么开始

1. 把模板整个拷出来当新项目的根目录（**项目名 ＝ 根目录名**）。
   中文项目用 `ai/template/`，其他语言用 **`ai/template-en/`**——两份内容一样，路径一字不差。
2. **开一个会话，第一句话就说「初始化项目」**（用你习惯的语言说，意思对就行）。
   它会认出自己是**维护席**，照 `docs/guide/project-init.md` 走：
   先用选择题问你**项目名**和**沟通语言**，然后把文档本地化成那门语言、带你填完五个空。
   **旧项目也走这一条**——把模板覆盖上去，它会拿 `ai/frame-manifest.txt` 分清
   「我们的 / 旧项目的 / 撞名的」三类，出一份迁移映射表给你确认，再迁移归档。
3. 开会话，各说一个词（`开发者` / `审查者` / `监督者` / `维护者`；要开 R&D 线就说 `研究者`，见 §八之二）。

模板里带着守门脚本（文档断链、写死的路径与 IP、收件簿有没有漏、bug 闭环断没断、模板有没有跟上），开箱可用。

---

## 八之二、R&D 线与研究席（**编外座位，四权结构不改**）

> 目录与编号规矩 `ai/RandD/README.md`；研究席手册 `ai/roles/researcher.md`。**要不要开这条线由需求方定**，开发线不受影响。

FRAME 假设**目标明确、设计接近成型**：任务有验收标准、审查有对照的规格、维护有成型的规矩。
一个**还没有名字的想法**没有这三样——硬用 FRAME 跑它，四席里有三席在空转，
而探索本身被「先写验收标准」卡死。所以另开一条**与开发线并行的常设支线**：**R&D 线**。

| | 开发线 | R&D 线 |
|---|---|---|
| 要回答的 | 这个东西做出来了没有 | 这个东西**值不值得做、做不做得成** |
| 人 | 需求方 · 开发席 · 审查席 · 维护席 | 需求方 · **研究席** · 监督席 |
| 单位 | `T-####` 交付物，审查席派 | `R-####` **问题**，🔴 **研究席自开自排自关**，终态是「有结论 / 做不到 / 放弃」 |
| 工作区 | `src/` `ai/tasks/` `ai/specs/` … | 🔴 **只有 `ai/RandD/`** |
| 终点 | 上线 | **毕业**：九条判据全绿 → 监督席核实 → 需求方拍板 → 监督席执行迁移 |

🔴 **研究席有座位，但没有一种权。** 它的产出在被监督席核实之前，效力等同「我觉得」——
这句话**直接落在既有的效力顺序里**（`需求方拍板 > 审查席验证过的事实 > 监督席的意见 > 任何人的「我觉得」`）。
所以**四权分立、Four-Role、框架四条边一个字都不改**；制衡研究席的是**需求方**和**可证伪的实验**。
**它也不影响正在跑的开发流程**：两条线并行，产品进了开发期，R&D 线照样在跑。

**监督席在这条线上有三项写权扩展**（与本文第三节不同，因为**这条线上没有审查席**——
没有规格可对照、没有实现可审查）：①直接审查研究席的收敛产出；②直接与需求方就毕业达成一致；
③**执行毕业迁移**与新仓库的 FRAME 初始化。**除此之外只读。**

**为什么要有「毕业」这道门**：R&D 欠的账会在开发中期以「这个当初没验过」的形式爆出来，
那时改的代价是十倍。所以 🔴 **不许「先毕业，缺的以后补」**，九条判据里最要紧的两条是
**不可逆决策列全了**与**每项技术选型只能是「验证过 / 有先例 / 明说是赌注」三者之一**——
**不许有第四种「应该没问题」**。

🔴 **北极星：毕业时能把整个过程重建出来。** 所有编号、索引、三轴互引、当轮追加都为这一条。
判据只有一句：**换一个全新的会话，只靠这些文件，能不能把毕业产出写出来。**

---

## 九、一句话记住它

**把协作状态从对话里搬到文件里；让写的人和判的人分开；让规矩有人养；让每句话都有落点、每条结论都有依据。**

剩下的规矩，都是这一句的展开。

---

<sub>AI 四权分立开发模式 · FRAME（Four-Role · Audited · Maintained · Evidence-based）</sub>
