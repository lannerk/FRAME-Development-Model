# Directory layout + what to sync when you change one thing

> **Required for a new project, and it gets changed along with the directories** — an out-of-date map of the tree is worse than none.

## Directories

`<one line per directory. The top-level table is in CLAUDE.md; the detail goes here>`

## What to sync when you change one thing

> This section is what the file is really worth: **which things are inherently paired**, so changing one means you must change the other.

| Changed this | Must sync | What happens if you don't |
|---|---|---|
| `<e.g. a frontend file>` | `<e.g. the prototype mirror + version number>` | `<e.g. the browser eats the cache, showing up as "I changed it and nothing happened">` |
| | | |
