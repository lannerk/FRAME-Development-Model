# Product vision

> **It is the evidence the decision ledger sits under**: when a decision conflicts with the vision, either change the decision or change the vision first (changing the vision needs the Requester's final call).

## What it does

`<one paragraph. Written for someone who doesn't know it; no technical words>`

## What it does not do

`<hard boundaries. This section is more useful than the one above — it blocks more requests than it accepts>`

## Will it still hold in ten years

`<which parts are implementation that will change, which parts are judgments that won't>`
